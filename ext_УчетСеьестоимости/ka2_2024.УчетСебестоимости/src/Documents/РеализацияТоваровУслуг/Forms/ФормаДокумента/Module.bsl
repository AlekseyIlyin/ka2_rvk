#Область ОбработчикиСобытийФормы

&НаКлиенте
Процедура усОбработкаВыбораПосле(ВыбранноеЗначение, ИсточникВыбора)
	Если усОбслуживаниеФормВызовСервера.усЭтоФормаДляОтображенияСтоимости(ИмяФормы) Тогда
		ДанныеСебестоимостиПоСтрокам = усОбслуживаниеФормВызовСервера.усОбновитьСебестоимостьМаржу(Объект);
		Для Каждого ДанныеСебестоимости Из ДанныеСебестоимостиПоСтрокам Цикл
			СтрокаТовары = Объект.Товары.НайтиПоИдентификатору(ДанныеСебестоимости.ИдентификаторСтрокиСебестоимость);
			Если СтрокаТовары <> Неопределено Тогда
				ЗаполнитьЗначенияСвойств(СтрокаТовары, ДанныеСебестоимости);
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
КонецПроцедуры

#КонецОбласти